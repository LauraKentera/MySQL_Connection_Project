# Database Configuration File
# This file stores the connection parameters required to connect to the MySQL database.
# Ensure these values are accurate to establish a successful database connection.

# JDBC URL for connecting to the MySQL database
# Format: jdbc:mysql://<host>:<port>/<database_name>
db.url = jdbc:mysql://localhost:3306/travel23

# MySQL username used for authentication
db.username = root

# MySQL password associated with the username (leave blank if no password is set)
db.password =
